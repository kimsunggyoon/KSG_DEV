<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://www.mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ksg.test.mapper.mbm.mbmMapper">
	<insert id="signUp_POST" parameterType="java.util.HashMap" >
		INSERT INTO MEMBERINFO (MEM_NO ,ID, PW, EMAIL, ADDRESS, PHONE,TXT_PW_SHA)
		VALUES (
			(SELECT NVL(MAX(MEM_NO),0) FROM MEMBERINFO)+1
			,#{ID}
			,#{PW}
			,#{EMAIL}
			,#{ADDRESS}
			,#{PHONE}
			,#{TXT_PW_SHA}
		)
	</insert>
	
	<select id="Select_Mem_Info" parameterType="java.util.HashMap" resultType="java.util.HashMap">
		SELECT *
		FROM MEMBERINFO
		ORDER BY MEM_NO ASC
	</select>
</mapper>